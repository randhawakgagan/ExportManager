

@model ExportManager.DBModel.LicenseEditView
@{
// Layout = null;
}




<!DOCTYPE html>
@Scripts.Render("~/bundles/jquery")
@Scripts.Render("~/bundles/jqueryval")
@Scripts.Render("~/bundles/jqueryui")


<script type="text/javascript">

    $(function () {

        //$("#mylink").on("click", function () {
        $("#formitem").submit(function(event) {

            event.preventDefault();
            //var expId = $(this).data('id');
            var form= $("#formitem");

            //@*var url = '@Url.Action("Additem", "Export")';*@
            var url = "/Licenses/Additem";

            var postData = form.serialize();

            $.post(url, postData, function (res) {
                alert(res.msg);
                $("#msg").text(res.msg);
                //$.each(res.msg, function (i, msg) {
                //    $("#msg").val(msg);;
                ////    //  alert(exp.exp_date);




                //});


               
            });

        });


    });

</script>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>


    

    <form id="formitem" method="post">
        <div class="panel panel-info">
            <div class="panel-heading">Add Country</div>
            <div class="panel-body">
                <div class="form-inline">
                    <div class="form-group">
                        <label>Select Country</label>
                        @Html.DropDownListFor(s => s.SelectedCountries, Model.Items, "Select")
                    </div>


                    <div class="form-group">

                        @Html.HiddenFor(s => s.Lic_id)
                    </div>

                    <div class="form-group">


                        <input type="submit" value="ADD" id="mylink1" class="btn-success" />
                        @*  @Html.ActionLink("ADD", "AddExportitem", new { exp_id = Model.exp_id, lic_id = Model.lic_id })*@

                    </div>
                </div>



                <div class="container">
                    <p class="alert-info" id="msg"></p>
                </div>

               </div>
            </div>
    </form>
</body>
</html>
